# This directory holds operator secrets (e.g. flag derivation keys).
# These files must NEVER be committed to version control.
# Add your secrets here and reference them from flake.nix with:
#
#   secret = builtins.readFile ./secrets/flag-key;
#
# See README.md for details.
